class Destination{
  double lat;
  double lng;
  String name,address,category;
  double distance ;
  int rate;

  Destination(this.lat, this.lng, this.name,this.distance, this.address, this.category,this.rate);
}